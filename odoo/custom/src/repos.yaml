# See https://github.com/Tecnativa/doodba#optodoocustomsrcreposyaml
./odoo:
  defaults:
    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
    # for a sane value of 100 commits)
    depth: $DEPTH_DEFAULT
  remotes:
    ocb: https://github.com/OCA/OCB.git
    odoo: https://github.com/odoo/odoo.git
    openupgrade: https://github.com/OCA/OpenUpgrade.git
  target: ocb $ODOO_VERSION
  merges:
    - ocb $ODOO_VERSION
    # Example of a merge of the PR with the number <PR>
    # - oca refs/pull/<PR>/head

account-analytic:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/account-analytic.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/306/head # scrap_analytic

account-financial-reporting:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/account-financial-reporting.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

account-payment:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/account-payment.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

l10n-thailand:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/l10n-thailand.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/138/head # l10n_th_check_scb_bank

maintenance:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/maintenance.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

manufacture:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/manufacture.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/499/head # manufacturing_order_type

mis-builder:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/mis-builder.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

multi-company:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/multi-company.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

partner-contact:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/partner-contact.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

product-attribute:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/product-attribute.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

purchase-workflow:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/purchase-workflow.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

reporting-engine:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/reporting-engine.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

sale-workflow:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/sale-workflow.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/1129/head # sale_procurement_group_by_commitment_date

server-backend:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/server-backend.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

server-tools:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/server-tools.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

server-ux:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/server-ux.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

stock-logistics-warehouse:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/stock-logistics-warehouse.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

stock-logistics-workflow:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/stock-logistics-workflow.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

web:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/web.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
